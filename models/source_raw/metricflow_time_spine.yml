version: 2

models:
  - name: metricflow_time_spine
    description: "MetricFlow time spine (one row per day)."
    time_spine:
      standard_granularity_column: date_day

    columns:
      - name: date_day
        description: "Daily date used as the standard granularity."
        granularity: day

